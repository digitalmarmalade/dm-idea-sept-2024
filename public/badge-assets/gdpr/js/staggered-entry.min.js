! function(a) {
    a.fn.visible = function(l) {
        var o = a(this),
            s = a(window),
            i = s.scrollTop(),
            e = i + s.height(),
            d = o.offset().top,
            n = d + o.height();
        return (!0 === l ? d : n) <= e && i <= (!0 === l ? n : d)
    }
}(jQuery);


var win = $(window),
    allMods = $(".staggered,.pop-in");
allMods.each(function(l, o) {
    (o = $(o)).visible(!0) && o.addClass("already-visible")
}), win.scroll(function(l) {
    allMods.each(function(l, o) {
        (o = $(o)).visible(!0) && o.addClass("come-in")
    }), $(window).scrollTop() < 50 ? $("body").removeClass("scrolled") : $("body").addClass("scrolled")
});